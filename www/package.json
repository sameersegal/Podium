{
  "name": "@podiumstack/www",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The marketing site at podiumstack.com — deliberately independent of the app",
  "license": "MIT",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/share-pages.mjs && node scripts/check-previews.mjs",
    "preview": "wrangler dev",
    "screenshots": "node scripts/screenshots.mjs",
    "og": "node scripts/og-cards.mjs",
    "links": "node scripts/deep-links.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "astro": "^7.2.0",
    "playwright": "^1.62.1",
    "wrangler": "^4.120.1"
  },
  "dependencies": {
    "posthog-js": "^1.416.0"
  }
}
