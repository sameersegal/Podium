{
  "name": "@podiumconf/www",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The marketing site at podiumstack.com — deliberately independent of the app",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler dev",
    "deploy": "astro build && wrangler deploy"
  },
  "devDependencies": {
    "astro": "^7.2.0",
    "wrangler": "^4.120.1"
  }
}
